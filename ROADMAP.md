# Roadmap

项目方向与规划。已完成的、近期要做的、明确不做的，都写在这里，防止跑偏。

## 已完成

- v1.0 ~ v2.2：11 个本地视觉工具 + 多轮识图 skill（描述 / OCR / 检测 / 分割 / 零样本 / 定位 / 裁切）
- 基准测试套件：可复现的量化精度测试（OCR / 颜色定位 / 模板匹配 / 裁切 / 输入校验）
- 英文 README + 仓库卫生（密钥、权重、真人照片、营销素材全部 gitignore）
- Issue 模板（Bug 报告 / 功能建议，中文）
- 多图支持（v2.3.0）：`analyze_image` 多图逐张分析 + `compare_images` 拼图对比，共 12 个工具
- CLI 兜底 + 全局 AGENTS 规范（v2.3.0）：不依赖客户端 MCP 注入，命令行动手可用；安装 skill 时自动装全局识图入口

## 近期规划（按优先级）

1. **OCR 提速**：PaddleOCR 当前 CPU 平均约 6 秒/张，是用户可感知的最大短板。思路：换更轻的识别模型/引擎（如 PP-OCRv5 mobile），或按图片大小分级选择引擎。

## 需要时再做（有真实需求再启动）

- **真实照片检测数据集**：用 `--dataset` 挂"真人照片 + 标准答案"量化数人精度。照片不入库（隐私），只入库格式模板与使用说明。

## 明确不做

- **WebUI 配置台**：项目定位是 MCP 工具，不需要图形界面
- **多模型 fallback 链**：本地通常只装 1~2 个视觉模型，已有 quick→detailed 回退覆盖主要场景，配置面膨胀不值得
- **云端视觉服务**：项目核心价值是本地优先、图片不出本机
- **网页 / UI 自动化解析**：超出"识图"范畴

## 给贡献者的建议

- 改代码前先跑测试：`python tests\test_server.py`
- 改精度相关逻辑（OCR / 检测 / 定位）后跑基准：`python benchmarks\run_benchmark.py`
- 新增 / 修改工具后必须重启客户端（工具列表在启动时加载）
