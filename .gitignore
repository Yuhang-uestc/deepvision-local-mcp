# 运行时缓存
.cache/
__pycache__/
*.pyc

# 模型权重：AGPL 许可，不随仓库分发（首次调用自动下载）
*.pt
mobileclip_blt.ts
mobileclip_*.pt

# 个人/测试图片（含真人照片，禁止入库）
detected_*.png
g2_*.png
meme_*.png
loop_demo_box*.png
box_people_1.png

# 本地配置与密钥（config.toml 含 DeepSeek API Key，绝不能提交）
*.toml
*.bak-*
.env

# 默认生成文件目录
outputs/

# 基准测试生成物（可复现，不入库）
benchmarks/generated/
benchmarks/report/
